---
target: ui/src
total_score: 33
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
timestamp: 2026-08-08T12-33-37Z
slug: ui-src
---
Method: dual-agent (A: r5 design review · B: r5 detector)

# Design Health Score — AgentDesk UI (ui/src) · Run 5(M14 後驗收)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | 完整訊號鏈 + 實測的 aria-live;dock badge/OS 通知涵蓋未聚焦視窗 |
| 2 | Match System / Real World | 3 | 文案自然;SESSIONS 硬編碼、未知錯誤仍英文原文 |
| 3 | User Control and Freedom | 3 | Esc/髒表單/armed/退隊全驗證;刪除與 merge 無 undo,丟棄的凍結變更在 UI 無可見復原路徑 |
| 4 | Consistency and Standards | 3 | 狀態語彙典範級;但三個 tablist 只有 TabStrip 實作 roving — 同一個 widget 契約一處守兩處破 |
| 5 | Error Prevention | 4 | 「一個被設計出來的系統,不是散落的確認框」— useArmed 摩擦與後果成比例,全實測 |
| 6 | Recognition Rather Than Recall | 3 | 主要動作可見;hover 隱藏鈕、j/k 只活在表裡 |
| 7 | Flexibility and Efficiency | 4 | 端到端實測:⌘E 循環、⌘←/→ 搬卡含焦點跟隨、j/k review、roving tabs、profiles、modes |
| 8 | Aesthetic and Minimalist Design | 3 | 靜態冷靜;topbar 堆了檢視器鈕+欄數+view 切換,卡片腳最多 4 鈕+刪除 |
| 9 | Error Recovery | 3 | FriendlyError 是對的模式但只認 3 種;toast slice(-2) 仍與自家註解矛盾 |
| 10 | Help and Documentation | 3 | 決策點提示 + ⌘/ 表;無搜尋式說明(對此受眾可接受) |
| **Total** | | **33/40** | **Good(82.5%)· 與 R4 持平,但 P1 類別絕跡** |

## Design Specificity Verdict

「署名之作,不可互換 — 沒有任何通用 dashboard 能原封不動穿上這份 stylesheet」。CSS「是被論證的,不是被貼上的」— 註解記錄 fg-faint 為何存在(且驗算屬實:5.08:1/4.59:1)、狀態邊為何豁免。文案「讀起來像跑 agent 的人寫的,不是事後翻譯的」。性格最薄的地方:modal 們是稱職但制式的堆疊表單。

**Deterministic scan**:CLI 連續第四輪 exit 0。瀏覽器引擎照例四項(已豁免 side-tab、11px pane-title/profiles 提示、10–16px 階梯、terminal host 的 clip — 後者「依構造而剪裁,是刻意的 overflow 架構」)。

## Overall Impression

分數持平在 33,但這輪的真正結果是**嚴重度塌陷**:R4 有 2 個 P1,R5 一個 P1 都沒有 — 全部發現是 4 個 P2 + 1 個 P3,而且集中在 ARIA 結構語意這一類(無名 dialog、role=button 巢套、tablist 契約不一)。M14 的全部修正被實測驗證:diff roving、file chips、accessible names 帶狀態、armed merge 重命名、環境面板契約。

## Priority Issues(全部 P2/P3)

- **[P2] 每個 modal 都是無名 dialog** — role=dialog 無 aria-labelledby(probe:labelled:false);yolo 確認對 SR 開啟時只叫「dialog」。修:Modal 用 useId 接 h2。→ /impeccable harden
- **[P2] role=button 容器巢套真按鈕** — session row/卡片把 <button> 包在 role=button 裡,無效 ARIA;鍵盤能用(實測)但 accessibility tree 對結構說謊。修:容器改 role=group,門改為第一個全幅按鈕。→ /impeccable harden
- **[P2] 三個 tablist 兩個破契約** — view 切換與檢視器 tabs 無 roving tabindex(TabStrip 有)。修:selected ? 0 : -1 + 檢視器補 6 行方向鍵。→ /impeccable harden
- **[P2] 權限模式 select 對明眼人也匿名** — 掃過對話框會把該列讀成兩個 agent 選擇。修:拆成兩個有標籤的欄位。→ /impeccable clarify
- **[P3] SESSIONS 硬編碼 + toast slice(-2) 靜默丟棄** — 第三次被點名了。→ /impeccable polish

## Persona 重點

**Alex**:核心迴圈 10 個鍵內(實測乾淨);缺欄內排序鍵、批次關閉、鍵盤切分 pane。**Sam**:實測全過的部分很紮實(aria-live zh 播報、狀態進 label、focus ring、reduced-motion、對比全過);剩下的是結構語意類。**Jordan**:「attempt」無行內定義;並行元件沒說在限什麼;首跑路徑其餘強。

## Minor

.waiting-banner/.modal label/.term-host 各定義兩次(檔案裡其他地方不容忍的堆積);.section-count 10px 低於自家 11px 樓地板;.ov-agent 被看板卡片借用(命名洩漏);review 送出前沒有組合訊息預覽(start 對話框有的誠實這裡沒有);已完成收合狀態不跨重啟。

## Questions

15+ session 時要不要標題搜尋?⌘E 該不該按阻塞時長排序?review 送出前的預覽?topbar 能不能把欄數收進 tab?鍵盤切分 pane(⌘⌥⇧+方向)?兩個 attempt 的並排比較到底住在哪?
