---
target: ui/src
total_score: 33
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
timestamp: 2026-08-08T11-54-45Z
slug: ui-src
---
Method: dual-agent (A: r4 design review · B: r4 detector)

# Design Health Score — AgentDesk UI (ui/src) · Run 4(M13 後驗收)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | 名副其實:dot+標籤、計時、tab 徽章、banner、呼吸卡、toast、aria-live、dock badge、OS 通知 — probe 驗證 |
| 2 | Match System / Real World | 3 | 文案流利;但旗艦 review 面板把 git plumbing 原文端給眼睛(index 1111111..、---/+++),SESSIONS 硬編碼 |
| 3 | User Control and Freedom | 3 | Esc、髒表單、4s 自動解除、退隊 — 全驗證;刪卡片仍無 undo,merge 除了 git 自身無法召回 |
| 4 | Consistency and Standards | 3 | 單一 Modal/狀態語彙 — 漂移:profile 刪除即存但編輯要按儲存;「等待」有三種說法且 en/zh 對同一個 key 不一致 |
| 5 | Error Prevention | 4 | Best-in-class:與後果成比例的 armed 確認(merge 用 warn 不用紅)、換 agent 重設 mode、未實測 CLI 給複製不給送出、髒對話框擋 backdrop — 全驗證 |
| 6 | Recognition Rather Than Recall | 3 | cheat sheet + 密集提示;mode select 仍無可見標籤,j/k 與 ⌘←→ 只有表裡知道 |
| 7 | Flexibility and Efficiency | 3 | 鍵盤組強(⌘E 循環「superb」);缺:規模化的過濾/搜尋、檢視器不可調寬、沒有把焦點送進看板的捷徑 |
| 8 | Aesthetic and Minimalist Design | 4 | 冷靜有紀律;唯一會動的是 blocked 琥珀色,正是 Operate 介面該有的樣子;降調永遠用 token 不用 opacity |
| 9 | Error Recovery | 3 | FriendlyError 是對的模式但只認 3 種;其他拒絕仍是英文原文;toast 第 4 個會擠掉最舊的(與註解矛盾) |
| 10 | Help and Documentation | 3 | ⌘/ 表 + 恰在決策點的提示;無可搜尋文件 |
| **Total** | | **33/40** | **Good(82.5%)** |

## Design Specificity Verdict

「署名之作,不可互換」— 雙軸看板、注意力調度器、終端機旁的抽屜、第二下點擊會說出後果的 armed merge。zh-TW 文案有真的聲音(「風險被關在這張卡裡」)。剩餘的通用感集中在一處:**diff 面板仍然渲染原始 `git diff` 管線輸出**,是產品最深的想法困在最通用的呈現裡。

**Deterministic scan**:CLI 連續第三輪 exit 0。瀏覽器引擎照例:已豁免的 side-tab、11px(pane-title、profiles 提示)、10–16px 階梯、main.main clip。自掃 artifact 剔除。

## Priority Issues(給下一輪)

- **[P1] diff 每行都是 tab stop** — 300 行 diff = 300 個 tab stop 擋在抽屜頭和 merge 腳之間(M12 給 commentable 行加 tabIndex=0 的二階效應)。修:roving tabindex — `<pre>` 維持唯一 tab stop(已有 j/k),行改 tabIndex=-1 程式移焦。→ /impeccable harden
- **[P1] 靜態的 blocked 狀態對 AT 隱形** — 卡片 aria-label={task.title} 把 ⚠ 等你授權 從 accessible name 剝掉(M12 加 label 的二階效應);aria-live 播過就沒了,tab 過看板聽到四張一模一樣的卡。修:label 帶上狀態,或 aria-describedby 指向 .board-card-state。→ /impeccable harden
- **[P2] review 面板的 git plumbing** — meta 行收成 sticky 檔名 chip(檔名 + +N/−N),原文收進既有的 details 模式。→ /impeccable clarify
- **[P2] 看板卡片沒有 blocked 時長** — 側欄有計時、triage 主面板沒有;三個 agent 卡住時排序要跨面板對照。修:needsYou 時 .board-card-state 加 等你授權 · 4m。→ /impeccable polish
- **[P2] 環境面板三合一 + 儲存模型不一致** — 刪 profile 即存、編輯要按儲存;分節(語言/設定檔/診斷)+ 統一儲存契約。→ /impeccable clarify

## Persona 重點

**Alex**:⌘2 切到看板但焦點沒跟過去,搬卡前先 Tab 長征;檢視器 460px 不可調;toast 第 4 個擠掉第 1 個(與註解矛盾);4+ agent 無搜尋。**Sam**:diff tab 拓撲(P1)、卡片 accessible name 缺狀態(P1)、role=button 容器包真按鈕違反 ARIA 巢套規則(Chromium 沒事,VoiceOver/NVDA 表單模式會出事);正面:focus ring 全域、Modal trap/還原、aria-live 含多重播報全數驗證。**Jordan**:mode select 無可見標籤;新卡對話框一次教五個 git 概念;正面:空狀態全部給指令、trust 提示在提問前先解釋。

## Minor

SESSIONS 硬編碼;「等待」三種說法且 en/zh 對 section key 不一致;toast 擠掉舊錯誤;⌘←→ 在表裡沒說要先聚焦卡片;板欄 <900px 橫向捲動;總覽已關閉卡顯示「不回報狀態」讀起來像錯誤;4s 自動解除偏快(6–8s 更寬容);timeline 長 prompt 無視覺分隔。

## Questions

卡片的 blocked 狀態能否帶成本(等你授權 · 4m,按等待時間排序)讓牆自己完成 triage?diff 若被允許不長得像 git diff(檔案 chip、hunk 摺疊、留言 gutter)會是什麼樣?視窗聚焦但眼睛在別的 pane 時,一聲可選的提示音是缺失的通道還是 scope creep?兩個 attempt 比較「誰贏了」的並排 diff 住在哪?看板要不要一個 AT 等價的 landmark(「3 張卡片等你」)?
