---
target: ui/src
total_score: 32
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
timestamp: 2026-08-08T11-21-40Z
slug: ui-src
---
Method: dual-agent (A: r3 design review · B: r3 detector)

# Design Health Score — AgentDesk UI (ui/src) · Run 3(驗收)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | 旗艦能力名副其實:dot → 活動行+計時 → banner → tab 徽章 → 呼吸卡 → dock badge → OS 通知 → aria-live。實測 ⌘E 一鍵落進被擋的 TUI |
| 2 | Match System / Real World | 3 | zh-TW 開發者語言道地誠實;但 attempt/worktree/yolo 行話牆仍在,側欄標題 SESSIONS 是唯一漏翻的英文 |
| 3 | User Control and Freedom | 3 | Esc、髒表單防護、armed 刪除/丟棄、退隊、eject — 但「合併回 main」單擊即發而較輕的丟棄反而有確認;看板搬卡無 undo |
| 4 | Consistency and Standards | 3 | 單一 Modal、單一狀態語彙 — 但總覽卡片要雙擊才開(看板單擊+Enter)、標題用 basename(cwd),同一個 session 在四個面板有三種行為 |
| 5 | Error Prevention | 3 | armed 刪除/丟棄、髒表單、換 agent 重設 mode — 被未防護的單擊 merge 和側欄 ✕ 直接關活終端機拖累 |
| 6 | Recognition Rather Than Recall | 3 | icon 鈕全有 label、快捷鍵印在 tooltip 和 ⌘/ 表 — 拖曳語法(搬卡、切分 pane)仍是純 recall |
| 7 | Flexibility and Efficiency | 4 | 實測全通:⌘E 循環、⌘1/2/3、⌘I、j/k+Enter、roving tabs、profiles、並行調度、拖曳佈局+zoom;終端機內 Shift 變體在產品內有文件 |
| 8 | Aesthetic and Minimalist Design | 3 | 靜態時冷靜、動畫只留給唯一重要的訊號 — 但環境面板把語言+profiles+診斷+PATH 四件事塞進一根 520px 柱子 |
| 9 | Error Recovery | 3 | 錯誤就地顯示、error toast 常駐、堆疊 — 但幾乎每則訊息以原始 String(e) 收尾,英文嵌在 zh-TW 句子裡,沒有下一步 |
| 10 | Help and Documentation | 3 | ⌘/ 表 + 決策點的優質就地提示;無可搜尋說明,首跑 onboarding 只有空狀態 |
| **Total** | | **32/40** | **Good(80%)** |

## Design Specificity Verdict

**LLM 評估**:「署名之作,無法換產品穿」— 雙軸看板、呼吸卡(全 app 唯一的動畫,含 reduced-motion fallback)、注意力調度器、生命週期之外的臨時 session 區、終端機旁的檢視抽屜、開始對話框的誠實警告、CSS 裡寫明 CJK 理由的 12px/11px 分層 — 每個 token 都有記錄在案的意圖。唯一的走音:總覽用 basename(cwd) 命名 session,違反 codebase 自己寫下的「一個 session 一個名字」。

**Deterministic scan**:CLI 連續第二輪 **exit 0 零發現**。瀏覽器引擎:main.main 的 overflow clip(潛在 tooltip 風險)、pane-title 與 profiles 提示的 11px、10–15px 扁平字級(桌面密度工具的真實量測)、已豁免的 side-tab(瀏覽器引擎不讀 CSS 註解 waiver,CLI 讀)。自我掃描 artifact 照例剔除。

## Overall Impression

三輪的軌跡成立:27 → 31 → 32。H1 和 H7 兩個 4 分是「實測過才給」的 — evaluator 自己跑了 probe 驗證 ⌘E、j/k、focus trap、aria-live。剩下的高價值問題收斂到一個清晰的主題:**後果與摩擦不成比例**(merge 比 discard 重卻沒確認)加上**看板的最後一哩對鍵盤關閉**(搬卡只能拖曳)。

## Priority Issues(留給下一輪)

- **[P1] 看板搬卡只能拖曳** — 鍵盤/AT 使用者能跑完整個 review 迴圈(留言、送出、merge)卻永遠無法把卡片歸檔。修:聚焦卡片上 ⌘←/→ 搬一欄,走現成的 onMove + aria-live。→ /impeccable harden
- **[P1] merge 單擊即發、discard 反而有確認** — 摩擦倒掛:merge 動的是每個提示都承諾 attempt 碰不到的 base 分支。修:merge 套用同一個 armed 模式。→ /impeccable harden
- **[P2] String(e) 原文錯誤** — 已知的拒絕(不是 git repo、分支不存在、worktree 髒)映射成可行動的 zh-TW 訊息,原文收進「詳細」。→ /impeccable clarify
- **[P2] 總覽卡片雙重身份違規** — 雙擊才開 + basename 標題。修:s.title + 單擊進入。→ /impeccable polish
- **[P2] aria-live 低報** — 同一批多個 session 進入等待只播第一個;blocked→blocked 轉換(確認資料夾→等你授權)不重播;播報文字永駐 live region(皆 probe 實測)。修:改追蹤 per-session 上次播報狀態。→ /impeccable harden

## Persona 重點

**Alex**:搬卡是全 app 唯一沒有快捷鍵的高頻動作;banner 點擊永遠跳 waiting[0](和 ⌘E 的循環行為不一致)。**Sam**:看板的一半(kanban)對鍵盤唯讀;正面:2px focus ring 全域實測 7.36:1、modal trap/還原、toast role、icon 全有 label。**Jordan**:行話牆仍在但空狀態、boot gate、危險動作的自我解釋讓他不至於被拋棄。

## Minor

SESSIONS 硬編碼英文(唯一 i18n 漏網);檢視已結束 attempt 時終端機側顯示「把 session 拖進來」的離題邀請;環境面板該分成 設定/診斷 兩節;OK toast 4s 自動消失無 hover 暫停;⌘/ 表漏了 zoom 與拖曳語法;merge/PR 進行中只有按鈕 disable 沒有「合併中…」;.term-host 二次定義的 cascade 靠順序;timeline 未知狀態字串直接渲染原文。

## Questions

摩擦是否該與後果成比例?等待訊號能否變成 burn-down 隊列?總覽的標題欄位到底是給什麼的?「merge 自動搬卡」是不是比搬卡快捷鍵更深的修法?環境面板要不要拆成 設定/診斷?
