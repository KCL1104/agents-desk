{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Marol main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-open",
    "notification:default",
    "opener:allow-open-url",
    "opener:allow-open-path",
    "updater:default"
  ]
}
